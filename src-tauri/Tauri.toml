# Minimal Tauri config accepted by the CLI
productName = "CazFightstick"
version = "1.0.0"
identifier = "com.cazfightstick.app"

[build]
# keep defaults — no custom commands
beforeBuildCommand = ""
beforeDevCommand = ""

[bundle]
active = true
targets = ["msi"]
